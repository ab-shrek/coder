#include <iostream>
#include<queue>
using namespace std;

int main()
{
	queue<int> p;
	long long a,b,c,d,e=0,f,g,h;
	cin>>a;
	bool q[a+1][a+1];
	for(b=0;b<=a;b++)
	{
		for(f=0;f<=a;f++)
		q[b][f]=false;
	}
	for(b=1;b<a;b++)
	{
		cin>>c>>d;
		e+=d;
		q[c][d]=true;
		
	}
}
